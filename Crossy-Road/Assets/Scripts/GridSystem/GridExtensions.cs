using System.Collections.Generic;

namespace Blink.KEK.RectangularSystem
{
    public static class GridExtensions
    {
        //public static IEnumerable<RectangularVector> GetCellCorners(this RectangularGrid rectangularGrid, RectangularGridCell cell)
        //{
        //    float x = cell.WorldPosition.X;
        //    float y = cell.WorldPosition.Y;
        //    float halfSize = rectangularGrid.CellSize / 2;

        //    yield return new RectangularVector(x - halfSize, y - halfSize);
        //    yield return new RectangularVector(x + halfSize, y - halfSize);
        //    yield return new RectangularVector(x - halfSize, y + halfSize);
        //    yield return new RectangularVector(x + halfSize, y + halfSize);
        //}
    }
}
